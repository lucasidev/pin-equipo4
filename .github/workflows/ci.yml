<<<<<<< HEAD
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # CLAVE: Esto permite que aparezca un botón para disparar el flujo manualmente en GitHub
  workflow_dispatch:
    inputs:
      confirm_destroy:
        description: 'Escribe "DESTROY" para confirmar la eliminación de la infraestructura'
        required: true
        default: 'NO'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  # =========================================================
  # TRABAJO 1: VALIDACIÓN ESTÁTICA
  # =========================================================
  validate:
    name: Validate infra (terraform + compose)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # No corremos validación si la intención explícita es destruir todo
    if: github.event.inputs.confirm_destroy != 'DESTROY'
    steps:
      - uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Validar formato del código
        run: terraform -chdir=terraform/aws fmt -check

      - name: Inicializar Terraform (Validación Local)
        run: terraform -chdir=terraform/aws init -backend=false

      - name: Validar sintaxis de los archivos .tf
        run: terraform -chdir=terraform/aws validate

<<<<<<< HEAD
=======
      # Fase CI: Se ejecuta en Pull Requests e inyecta la llave SSH para el Plan
      - name: Planificar cambios (Terraform Plan)
        if: github.event_name == 'pull_request'
        env:
          TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }} # <-- Inyección de seguridad
        run: terraform -chdir=terraform/aws plan

      # Fase CD: Se ejecuta al fusionar en main e inyecta la llave SSH para el Apply
      - name: Aplicar cambios en AWS (Terraform Apply)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }} # <-- Inyección de seguridad
        run: terraform -chdir=terraform/aws apply -auto-approve
>>>>>>> 1fd5e1a (Resolviendo error con clave ssh y nombre de recurso EC2)
      - name: Validate docker compose
        run: docker compose -f compose/docker-compose.yml config > /dev/null

  # =========================================================
  # TRABAJO 2: PLANIFICACIÓN EN AWS (Fase CI - Pull Requests)
=======
  # =========================================================
  # TRABAJO 2: PLANIFICACIÓN EN AWS (Usa llaves estáticas de tus Secrets)
>>>>>>> d55b40e (ci: pipeline completo con autenticacion estatica de aws y despliegue docker)
  # =========================================================
  plan-aws:
    name: Terraform plan (AWS)
    runs-on: ubuntu-latest
    needs: validate
<<<<<<< HEAD
    if: github.event_name == 'pull_request' && github.event.inputs.confirm_destroy != 'DESTROY'
=======
    if: github.event_name == 'pull_request'
>>>>>>> d55b40e (ci: pipeline completo con autenticacion estatica de aws y despliegue docker)
    steps:
      - uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

<<<<<<< HEAD
=======
      # Reemplazamos la acción compleja de OIDC por la inyección directa de tus llaves
>>>>>>> d55b40e (ci: pipeline completo con autenticacion estatica de aws y despliegue docker)
      - name: Terraform init (aws)
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: "us-east-1"
        run: terraform -chdir=terraform/aws init

      - name: Terraform plan (aws)
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: "us-east-1"
          TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
        run: terraform -chdir=terraform/aws plan -no-color

  # =========================================================
<<<<<<< HEAD
  # TRABAJO 3: DESPLIEGUE EN AWS (Fase CD - Push a Main)
=======
  # TRABAJO 3: DESPLIEGUE EN AWS (Usa llaves estáticas de tus Secrets)
>>>>>>> d55b40e (ci: pipeline completo con autenticacion estatica de aws y despliegue docker)
  # =========================================================
  apply-aws:
    name: Terraform apply (AWS)
    runs-on: ubuntu-latest
    needs: validate
<<<<<<< HEAD
    if: github.ref == 'refs/heads/main' && github.event_name == 'push' && github.event.inputs.confirm_destroy != 'DESTROY'
=======
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
>>>>>>> d55b40e (ci: pipeline completo con autenticacion estatica de aws y despliegue docker)
    steps:
      - uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Terraform init (aws)
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: "us-east-1"
        run: terraform -chdir=terraform/aws init

      - name: Terraform apply (aws)
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: "us-east-1"
          TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
        run: terraform -chdir=terraform/aws apply -auto-approve
<<<<<<< HEAD

  # =========================================================
  # TRABAJO 4: DESTRUCCIÓN SEGURA DE INFRAESTRUCTURA (Manual)
  # =========================================================
  destroy-aws:
    name: Terraform destroy (AWS)
    runs-on: ubuntu-latest
    # Solo corre si lo disparas manualmente Y escribes la palabra de confirmación exacta
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.confirm_destroy == 'DESTROY'
    steps:
      - uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Terraform init (aws)
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: "us-east-1"
        run: terraform -chdir=terraform/aws init

      - name: Ejecutar destrucción completa
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: "us-east-1"
          TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
        run: terraform -chdir=terraform/aws destroy -auto-approve
=======
>>>>>>> d55b40e (ci: pipeline completo con autenticacion estatica de aws y despliegue docker)
