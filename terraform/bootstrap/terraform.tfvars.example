# Copy to terraform.tfvars and adjust if needed. terraform.tfvars is gitignored.
# The defaults work as-is; override only to change region or bucket name.

# aws_region        = "us-east-1"
# state_bucket_name = "pin-equipo4-tfstate"
