# Copy to compose/.env and fill with real secrets. compose/.env is gitignored.

# Mongo root credentials (used by mongo init and by the api connection string).
MONGO_ROOT_USER=pokedex
MONGO_ROOT_PASSWORD=change-me-to-a-real-secret

# Redis password (AUTH enabled for prod parity).
REDIS_PASSWORD=change-me-to-a-real-secret

# API auth.
JWT_SECRET=change-me-to-a-long-random-string-min-32-chars
ADMIN_NAME=admin
ADMIN_EMAIL=admin@pokedex.local
ADMIN_USERNAME=pokeadmin
ADMIN_PASSWORD=change-me-min-8-chars

# Grafana admin.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me

# Per-IP request cap per minute on the api. Defaults to 120 (production-ish).
# Raise it (e.g. 2000) when running the k6 load test, which drives all
# traffic from a single IP and would otherwise hit the limit.
RATE_LIMIT_MAX=2000

# Host ports. `just up` runs scripts/ensure-ports.mjs first, which picks a
# free port automatically if one of these is already taken.
API_HOST_PORT=3000
PROMETHEUS_HOST_PORT=9090
GRAFANA_HOST_PORT=3001
